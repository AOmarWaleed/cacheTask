import { StatusFiltrationPipe } from './status-filtration.pipe';

describe('StatusFiltrationPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusFiltrationPipe();
    expect(pipe).toBeTruthy();
  });
});
