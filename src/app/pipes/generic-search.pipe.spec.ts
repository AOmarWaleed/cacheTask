import { GenericSearchPipe } from './generic-search.pipe';

describe('GenericSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
